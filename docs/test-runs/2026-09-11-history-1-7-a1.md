# History architecture 1–7 report — 2026-09-11 (a1)

## Result

Code, tests, and push/deployment status are recorded from actual commands below. No secret/token/cookie/chat body was read; no real model call, load test, DB migration/schema change, ANALYZE, or VACUUM was performed.

## Baseline and repository safety

- Functional baseline: `b0a8b2886837a1b900c9b4cce42f72a4fbdf38e2`; report baseline/starting HEAD: `4924d72943f29196b67e66a529d5c03464373529`.
- Branch `feat/tasks`; `origin/feat/tasks` was fetched and matched starting HEAD before commit.
- Existing untracked Android/docs files were preserved and excluded from this commit.
- No pull/clean/stash/reset/rebase was used.

## 1–7 actual outcome

1. **Completed safe subset.** `GET /v1/agent/sessions/:id` defaults to 30 entries (max 200), presentation messages cap at 64 KiB, tool results at 24 KiB, and projected message JSON at 512 KiB. `truncated`, `content_truncated`, and `result_truncated` are explicit. Entry `id`, `seq`, and role remain unchanged.
2. **Completed safe subset.** Presentation-only projection shortens text/tool payloads; raw session entries remain unchanged. Tool id/name/status remain visible so a shortened result is not represented as a different call. Existing display semantics continue to omit standalone tool-result rows and attach them to the immediately preceding assistant entry.
3. **Completed on existing history detail page.** SessionsPage exposes `Load older messages`, uses exclusive `before_seq`, stable id deduplication, sequence ordering, bounded 30-entry pages, `has_more`, cursor, loading disable, and visible error state. It does not claim automatic scroll-top loading; explicit existing-page interaction was chosen as the lower-risk verifiable subset.
4. **Completed safe subset.** Fixed `RestoredMessage` JSON tags so `id` and `seq` are distinct and stable. Existing `around_seq`, `before_seq`, `after_seq`, and account authorization remain. Content is never used for deduplication.
5. **Verified.** `openSession` calls full `Store.LoadEntries` and constructs model messages from every persisted entry before runtime transforms. Display projection is only called by session detail response construction. No summary/pagination deletes transcript history.
6. **Not newly implemented.** Current runtime has a per-request `TransformContext` seam, but no already-proven pruning policy that safely rewrites old tool pairs across all providers. A speculative implementation was deliberately not added. Raw history remains intact. This item remains a documented follow-up.
7. **Existing safe compaction retained and verified by suite.** Chat config derives a known context window then uses `smartCompactionSettings`; runtime threshold/overflow compaction preserves tool boundaries, writes a checkpoint only after success, emits observable events/telemetry, and leaves the original context unchanged on failure. No prompt/schema changes were made.

## Tests and metrics

- `go test ./...`: PASS.
- `npm test -- --run`: PASS — 17 files, 63 tests.
- `npm run build`: PASS; existing Vite >500 KiB chunk warning remains.
- `git diff --check`: PASS.
- New Go tests cover presentation byte limits/markers, UTF-8 truncation, stable id+seq, HTTP response budget, and cross-account 404 isolation.
- Existing tests cover cursor bounds, stable frontend id dedup/order, compaction tool-boundary behavior, threshold activation, and failure preservation.
- Isolation performance evidence: bounded SQL pages (`limit+1`, max 200) and bounded projection response; no pressure/load benchmark was run per instruction.

## Compatibility and design tradeoffs

- Additive response fields are backward compatible.
- Correcting the accidental duplicate JSON key (`seq` had been serialized as `id`) restores the intended web contract.
- Original payloads remain authoritative; display projections are not accepted back by POST chat and never enter model context.
- Explicit older-page loading avoids surprising scroll jumps and is testable on the current history page; automatic upward-scroll anchoring remains unclaimed.

## Commit / deployment / rollback evidence

## Commit and remote push

- Commit: `33350c55ac08476ba6bc5c56bbaa378719659577` (`feat: bound and page history display`).
- Ordinary push completed; `origin/feat/tasks` resolved to `33350c55ac08476ba6bc5c56bbaa378719659577`.
- `git diff --check` passed before commit.

## Deployment status

- A strict SSH probe using the required PTY/key/host-key options succeeded once (`SSH_OK`).
- The subsequent required remote preflight command (repository state, protected paths, service state, health) timed out/SIGTERM-terminated twice. No remote repository state was trusted from the incomplete command.
- **BLOCKED:** independent staging build, remote SHA256 manifest, binary/index backup, atomic replace, service restart, health probes, and online regression were not executed. No service was restarted; OpenClaw and Caddy were not touched.
- Therefore production functionality is not claimed. Rollback evidence does not exist because no deployment switch occurred.
